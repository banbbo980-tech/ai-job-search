# Upstream Update Report: 2026-07-12 to 8bee3dd

## Scope

Temporary sync branch: `sync/upstream-2026-07-12-8bee3dd`

Stable branch left unchanged: `codex-migration`

Official range reviewed: `c134eef5530e5431707ab553582a452be2306041..8bee3ddb9731b715908dbde288f457e541b3d7dc`

## Official Commits

| Commit | Official change | Upstream files |
|---|---|---|
| `22d8b48` | Add Trendshift #1 repository-of-the-day badge. | `README.md` |
| `543d1a7` | Add no-cryptocurrency/no-token disclaimer. | `README.md` |
| `569b1df` | Harden security guard handling of malformed JSON structures. | `tools/security_guards.py`, `tests/test_security_guards.py` |
| `379abad` | Clarify custom-template `--use` activation metadata. | `.claude/commands/add-template.md` |
| `8bee3dd` | Clean all custom-template test compilation artifacts. | `.claude/commands/add-template.md` |

## Conversion Decisions

The upstream Git history was preserved by merging `upstream/master` into this
temporary branch. The only merge conflict was in `README.md`; it was resolved by
keeping the Codex-native README structure, requirements, and privacy language
while adding the official Trendshift badge and a Codex-compatible no-token
disclaimer.

The legacy `.claude/commands/add-template.md` was kept with the official
upstream edits for parity comparison. Active workflow behavior was converted
semantically into `.agents/skills/add-document-template/SKILL.md`; the Codex
skill now documents switch-mode metadata resolution, ambiguous-template stops,
`template.tex` validation, managed active-template replacement, `--use default`,
and cleanup of all `_compile_test.*` outputs.

The official security guard fix was imported directly into
`tools/security_guards.py` and expanded with Codex-side tests for missing
optional permission/script objects, null script shapes, invalid manifest JSON,
and sync-branch update checking.

The upstream checker now permits the documented
`sync/upstream-YYYY-MM-DD-<sha>` branch pattern while still rejecting unrelated
feature branches. This keeps the sync workflow testable before merge.

## Feature Parity

| Official change | Codex equivalent | Tests or checks | Result | Remaining risk |
|---|---|---|---|---|
| Trendshift badge in README | Badge retained in Codex README and still points to the official `MadsLorentzen/ai-job-search` Trendshift page. | Manual README diff review. | Preserved. | External Trendshift badge availability is outside repo control. |
| No crypto/no token disclaimer | Codex README states there is no affiliated cryptocurrency, token, coin, or paid sponsorship program, and that official coin/token claims are fraudulent or unaffiliated. | Manual README diff review. | Preserved with Codex wording. | None known. |
| Security guard rejects malformed `.claude/settings.json` shapes | Guard validates top-level object, `permissions` object, and string-list `permissions.allow`. | `tests/test_security_guards.py`; `python tools/security_guards.py`. | Preserved and expanded. | None known. |
| Security guard rejects malformed `.agents/**/package.json` shapes | Guard validates manifest top-level object and `scripts` object before lifecycle-script checks. | `tests/test_security_guards.py`; `python tools/security_guards.py`. | Preserved and expanded. | None known. |
| `--use <name>` resolves registered template metadata before activation | `$add-document-template` switch mode reads `TEMPLATE.md`, verifies `template.tex`, derives kind from path, and activates without re-running registration. | `tests/test_codex_workflow_contracts.py`; `python tools/codex_compatibility.py`. | Preserved in Codex skill. | Workflow is instruction-level; end-to-end LaTeX execution remains environment-blocked locally. |
| Ambiguous or missing template names stop safely | `$add-document-template` stops on no match or multiple matching manifests and asks for list/rename action. | `tests/test_codex_workflow_contracts.py`; `python tools/codex_compatibility.py`. | Preserved. | None known. |
| Test compile cleanup includes every generated artifact | `$add-document-template` requires deleting named `_compile_test` files plus any other `_compile_test.*` byproducts. | `tests/test_codex_workflow_contracts.py`; `python tools/codex_compatibility.py`. | Preserved. | Local compile cleanup could not be exercised without LaTeX. |
| State checker supports documented sync branch flow | `tools/check_upstream_updates.py` allows stable `codex-migration` or a properly named sync branch. | `tests/test_upstream_update_check.py`. | Added to keep sync workflow runnable. | None known. |

## Files Changed

Official upstream merge changed:

- `.claude/commands/add-template.md`
- `README.md`
- `tests/test_security_guards.py`
- `tools/security_guards.py`

Codex conversion changed:

- `.agents/skills/add-document-template/SKILL.md`
- `tests/test_codex_workflow_contracts.py`
- `tools/codex_compatibility.py`
- `tests/test_security_guards.py`
- `tools/check_upstream_updates.py`
- `tests/test_upstream_update_check.py`
- `docs/upstream-state.json`
- `docs/upstream-updates/2026-07-12_8bee3dd.md`

No files were removed. No personal document folders or generated application
files were added.

## Verification Results

Available checks run before advancing `docs/upstream-state.json`:

| Command | Result |
|---|---|
| `python -m unittest discover -s tests -t . -v` | PASS: 87 tests in 32.819s. |
| `python tools/lint_skills.py` | PASS: `lint_skills: OK (22 skills, 9 commands, settings.json)`. |
| `python tools/security_guards.py` | PASS: `security_guards: OK (permissions allowlist, gitignore rules, package manifests)`. |
| `python tools/codex_compatibility.py` | PASS: `codex_compatibility: OK (13 workflow skills, 8 shared references, AGENTS.md)`. |
| `python tools/check_upstream_updates.py --no-fetch` | PASS before state update: reported the expected five upstream commits and Claude-specific template command requiring conversion. |
| `npm --prefix .agents/skills/freehire-search/cli run typecheck` | PASS. |
| `npm --prefix .agents/skills/jobbank-search/cli run typecheck` | PASS. |
| `npm --prefix .agents/skills/jobdanmark-search/cli run typecheck` | PASS. |
| `npm --prefix .agents/skills/jobindex-search/cli run typecheck` | PASS. |
| `npm --prefix .agents/skills/jobnet-search/cli run typecheck` | PASS. |
| `npm --prefix .agents/skills/linkedin-search/cli run typecheck` | PASS. |

Post-state verification after this report and `docs/upstream-state.json` were
updated:

| Command | Result |
|---|---|
| `python -m unittest discover -s tests -t . -v` | PASS: 87 tests in 31.946s. |
| `python tools/lint_skills.py` | PASS: `lint_skills: OK (22 skills, 9 commands, settings.json)`. |
| `python tools/security_guards.py` | PASS: `security_guards: OK (permissions allowlist, gitignore rules, package manifests)`. |
| `python tools/codex_compatibility.py` | PASS: `codex_compatibility: OK (13 workflow skills, 8 shared references, AGENTS.md)`. |
| `python tools/check_upstream_updates.py --no-fetch` | PASS: recorded official commit and `upstream/master` both equal `8bee3ddb9731b715908dbde288f457e541b3d7dc`; status up to date. |

## Environment-Blocked Checks

| Tool | Local result | Blocked checks |
|---|---|---|
| Bun | `bun --version` failed: command not found. | Official `bun install`, `bun run typecheck`, and `bun test --timeout 30000` portal checks. |
| LaTeX `lualatex` | `lualatex --version` failed: command not found. | CV compile smoke test and custom-template compile execution. |
| LaTeX `xelatex` | `xelatex --version` failed: command not found. | Cover-letter compile smoke test and custom-template compile execution. |
| Poppler `pdftotext` | `pdftotext -v` failed: command not found. | ATS text-layer extraction and reading-order checks. |

These remain documented prerequisites; no global software was installed.

## Security Review

`python tools/security_guards.py` passed after importing the upstream JSON shape
hardening. The change preserves personal-data ignore rules, `.claude`
permission allowlist checks, and lifecycle-script blocking for portal CLI
package manifests.

No push was performed. Nothing was pushed to `upstream`.

## Conflict Resolution

`README.md` conflicted during the merge because the Codex README had already
replaced Claude-specific setup guidance. Resolution kept Codex requirements and
workflow language, kept attribution to the original project, and added only the
official badge/disclaimer functionality in Codex-compatible wording.

## Remaining Limitations

- Local production document verification is incomplete until Bun, LaTeX, and
  Poppler are installed.
- The template workflow parity is instruction- and contract-tested locally, but
  end-to-end PDF cleanup cannot be executed on this machine without LaTeX.
- This branch has not been merged into `codex-migration` and has not been
  pushed.
