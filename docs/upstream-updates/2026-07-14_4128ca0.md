# Upstream Update Report: 2026-07-14 through 4128ca0

## Summary

- Previous integrated official commit: `99cf1000d69b53c39a247158bf462f0ad3d7eed2`
- New official upstream commit: `4128ca03189e19f9e2e54a5be63653f6065dbb87`
- Temporary branch: `sync/upstream-2026-07-14-4128ca0`
- Integration mode: merge official `upstream/master` into the temporary Codex branch, then add Codex-native adaptations.
- Push status: not pushed.

## Official Commits Integrated

| Commit | Official change | Codex conversion |
| --- | --- | --- |
| `e341d19` | Validate salary-data shape before lookup. | Preserved `salary_lookup.py` and salary tests; Codex workflows still treat salary data as optional local private data. |
| `160b479` | Add Poppler-based PDF page-count/text-layer verification in CI. | Preserved `tools/verify_pdf.py`, tests, and CI checks while retaining Codex compatibility checks. |
| `a03529f` | Harden skill/settings linting against malformed shapes. | Preserved `tools/lint_skills.py` and `tests/test_lint_skills.py`; retained Codex skill lint coverage. |
| `fca1c42` | Document `needspace` in minimal TeX installs. | Added the same prerequisite to Codex `SETUP.md` without adding Claude or Anthropic requirements. |
| `03798ec` | Add Jobbank and Jobdanmark CLI contract/parsing tests. | Preserved new tests under `.agents/skills/<portal>/cli/tests/`; portal CLIs remain Codex skill tools. |
| `b20467d` | Add framework-version markers, CI guard, and fork update checker. | Kept legacy Claude version markers, added equivalent markers to active Codex reference files, and combined the official framework-version advisory into the Codex upstream checker. |
| `0a8fc19` | Sync onboarding docs with `linkedin-search` and `freehire-search` reality. | Updated active Codex `$job-setup`, `$add-job-portal`, and search-query references with portal auto-discovery and fallback wording. |
| `4128ca0` | Detect salary company columns by header token rather than exact match. | Preserved salary conversion behavior and tests. |

## Conflicts Resolved

| File | Resolution |
| --- | --- |
| `.github/workflows/ci.yml` | Combined Codex lint/compatibility/security jobs with official `fetch-depth: 0`, framework version guard, Poppler install, and `tools/verify_pdf.py` PDF text checks. |
| `SETUP.md` | Kept Codex as the active setup surface and added official Bun package-manager note, minimal TeX package list including `needspace`, and Poppler guidance. |
| `tools/check_upstream_updates.py` | Kept the Codex read-only commit/state checker and added official framework-version comparison as advisory output. |

## Claude-Specific Changes Converted

| Official Claude change | Active Codex equivalent |
| --- | --- |
| `.claude/commands/add-portal.md` explains `freehire-search`, portal CLI auto-discovery, and optional `site:` fallback registration. | `.agents/skills/add-job-portal/SKILL.md` now explains auto-discovery and optional fallback placeholders in `job-application-core/references/search-queries.md`. |
| `.claude/commands/setup.md` updates the portal question for `linkedin-search`, `freehire-search`, Danish demos, and local portal additions. | `.agents/skills/job-setup/SKILL.md` now asks the same Codex-native portal configuration question. |
| `.claude/skills/job-scraper/search-queries.md` clarifies CLIs are primary and `site:` queries are fallback. | `.agents/skills/job-application-core/references/search-queries.md` now carries the same active Codex behavior. |
| `.claude/skills/job-application-assistant/*` adds `framework_version: 1.0.0`. | `.agents/skills/job-application-core/references/01-07*.md` now include matching `framework_version: 1.0.0` markers. |

## Feature Parity

| Area | Status | Notes |
| --- | --- | --- |
| Setup/profile workflow | Preserved | Active via `$job-setup`; no invented-candidate-data rules weakened. |
| Search/deduplication | Preserved | `$job-search` still auto-discovers portal CLIs; fallback search-query guidance updated. |
| Ranking/evaluation | Preserved | Existing evaluation references retained with framework version marker. |
| Apply/reviewer/PDF/ATS workflow | Preserved | Reviewer subagent/fallback and local-only generated files remain active. |
| Portal tools | Preserved and expanded | New Jobbank/Jobdanmark tests integrated. |
| Salary tools | Preserved and hardened | Official shape/header fixes integrated. |
| CI and verification | Preserved and expanded | Codex compatibility plus official PDF/framework checks. |
| Privacy safeguards | Preserved | Generated and personal-data paths remain ignored. |

## Verification Plan

Run:

```bash
python -m unittest discover -s tests -t . -v
python tools/lint_skills.py
python tools/security_guards.py
python tools/codex_compatibility.py
python tools/check_upstream_updates.py --no-fetch
git diff --check
```

Run Bun install/typecheck/test for:

- `jobbank-search`
- `jobdanmark-search`
- `jobindex-search`
- `jobnet-search`
- `linkedin-search`
- `freehire-search`

Run LaTeX/Poppler verification:

- Compile `cv/main_example.tex` twice with `lualatex`.
- Compile `cover_letters/cover_example.tex` twice with `xelatex`.
- Confirm CV is 2 pages and cover letter is 1 page.
- Render and visually inspect every page.
- Run `pdftotext -layout` and check contact details, headings, reading order, and broken glyph artifacts.

## Remaining Risk Before Merge

This branch is not ready to merge until full local verification completes and
`parity_test_status` is updated from `pending-sync-branch-verification` to a
passed status.
