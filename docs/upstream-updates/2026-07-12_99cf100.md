# Upstream Sync Report: 99cf100

Date: 2026-07-12

Stable Codex branch: `codex-migration`

Temporary sync branch: `sync/upstream-2026-07-12-99cf100`

Previous integrated official commit: `855b6725f34b52aa869a6d7e5fe39103dc56485f`

New integrated official target: `99cf1000d69b53c39a247158bf462f0ad3d7eed2`

Verification status: passed local verification on the temporary sync branch.

## Official Commits

| Commit | Official change | Codex handling |
|---|---|---|
| `1526afc` | Adds the maintainer's story to `README.md`. | Preserved the story as a Codex-compatible README section. The text attributes the experience to the original maintainer, avoids first-person claims by this fork, and does not restore Claude or Anthropic runtime requirements. |
| `ff78dae` | Extracts Jobnet CLI pure helpers and adds fixture/mock normalization tests. | Preserved the helper exports and tests under `.agents/skills/jobnet-search/cli`, keeping the Jobnet portal inside the Codex skill structure. |
| `99cf100` | Updates CI to run CLI fixture/mock test suites when portal tests are present. | Preserved the Bun fixture/mock test runner and adapted CI wording to Codex/fork terminology. |

## File Inventory

| Path | Status | Category | Codex/security notes |
|---|---:|---|---|
| `.agents/skills/jobnet-search/cli/src/commands/detail.ts` | Modified | Portal CLI | Adds exported plain-detail formatting helper for testable formatting behavior. |
| `.agents/skills/jobnet-search/cli/src/commands/search.ts` | Modified | Portal CLI | Adds exported search parameter/output helpers and keeps live API behavior unchanged. |
| `.agents/skills/jobnet-search/cli/tests/detail-formatting.test.ts` | Added | Portal CLI test | Network-free Bun test using fictional fixture data. |
| `.agents/skills/jobnet-search/cli/tests/search-normalization.test.ts` | Added | Portal CLI test | Network-free Bun test using fictional fixture data. |
| `.github/workflows/ci.yml` | Modified | CI | Runs Bun fixture/mock tests when present; comments adapted for Codex fork safety. |
| `README.md` | Modified | Documentation | Adds maintainer story while preserving Codex as the active user guide. |
| `cv/main_example.tex` | Modified | Template/ATS | Uses text contact labels and hyphen list markers so `pdftotext` does not extract moderncv private-use icon glyphs. |
| `tests/test_latex_template_contracts.py` | Modified | Test | Adds a contract test for ATS-safe moderncv contact and list symbols. |

## Conflict Resolution

`README.md` conflicted because the Codex migration had replaced the original
Claude-oriented README while upstream added a new maintainer-story section. The
resolution keeps the Codex README as the active user guide and adds the official
story with third-person attribution to the original maintainer.

No Jobnet or CI merge conflicts occurred.

## Codex Conversion

- No new Claude command or Claude skill was introduced.
- No Claude or Anthropic runtime requirement was restored.
- README language was converted from original slash-command framing to Codex
  workflow wording.
- CI comments were converted from Claude Code permission language to Codex fork
  workflow and privacy/security language.
- Jobnet behavior was preserved in `.agents/skills/jobnet-search/cli`, the
  active Codex portal skill location.
- PDF/ATS verification found an existing example-CV text-layer issue from
  `moderncv` icons and hollow list markers. The fix keeps the visual template
  clean while making the text extraction ATS-safe.

## Verification Results

| Check | Result |
|---|---|
| `python -m unittest discover -s tests -t . -v` | Passed: 91 tests in 45.129s |
| `python tools/lint_skills.py` | Passed: `lint_skills: OK (22 skills, 9 commands, settings.json)` |
| `python tools/security_guards.py` | Passed: `security_guards: OK (permissions allowlist, gitignore rules, package manifests)` |
| `python tools/codex_compatibility.py` | Passed: `codex_compatibility: OK (13 workflow skills, 8 shared references, AGENTS.md)` |
| `python tools/check_upstream_updates.py --no-fetch` | Passed after the sync-state docs were committed: recorded and upstream commit both `99cf1000d69b53c39a247158bf462f0ad3d7eed2` |
| `bun install && bun run typecheck && bun test --timeout 30000` for `jobbank-search` | Passed: 1 Bun test |
| `bun install && bun run typecheck && bun test --timeout 30000` for `jobdanmark-search` | Passed: 1 Bun test |
| `bun install && bun run typecheck && bun test --timeout 30000` for `jobindex-search` | Passed: 6 Bun tests |
| `bun install && bun run typecheck && bun test --timeout 30000` for `jobnet-search` | Passed: 6 Bun tests, including the new official fixture tests |
| `bun install && bun run typecheck && bun test --timeout 30000` for `linkedin-search` | Passed: 17 Bun tests |
| `bun install && bun run typecheck && bun test --timeout 30000` for `freehire-search` | Passed: 26 Bun tests |
| `lualatex --disable-installer --halt-on-error --interaction=nonstopmode cv/main_example.tex` twice | Passed: `main_example.pdf` compiled to exactly 2 pages |
| `xelatex --disable-installer --halt-on-error --interaction=nonstopmode cover_letters/cover_example.tex` twice | Passed: `cover_example.pdf` compiled to exactly 1 page |
| Poppler render and visual inspection | Passed: CV pages 1-2 and cover page render cleanly, with contact details and signature visible |
| `pdftotext -layout` ATS extraction | Passed: name/placeholders, email, phone, headings, and template terms extract cleanly; no `(cid:)`, replacement characters, broken icon glyphs, or broken bullet glyphs |

Non-fatal LaTeX warnings observed:

- `inputenc` is ignored under LuaLaTeX.
- `hyperref` removes subscript tokens from the example CV PDF title placeholder.
- `cover.cls` declares `deedy-resume-openfont`.
- `xcolor` reports obsolete `usenames` options in the cover template.
- The cover template substitutes a Raleway bold font shape.

None of these warnings blocked compilation, page counts, visual layout, or ATS
text extraction.

## Remaining Risk

No known blocker remains on the temporary branch. The branch is safe to merge
into `codex-migration` after user approval, provided `origin/codex-migration`
has not changed unexpectedly.
