# Upstream Sync Report: 855b672

Date: 2026-07-12

Stable Codex branch: `codex-migration`

Combined sync branch: `sync/upstream-2026-07-12-855b672`

Previous integrated official commit: `8bee3ddb9731b715908dbde288f457e541b3d7dc`

New integrated official target: `855b6725f34b52aa869a6d7e5fe39103dc56485f`

Verification status: passed production verification on the combined sync branch.

## Official Commits

| Commit | Official change | Codex handling |
|---|---|---|
| `09f0417` | Introduced Pip courier-bird branding, mascot assets, README header image, asset tests, and brand planning docs. | Audited all files. Final target keeps only the mascot GIF and README asset test after the later cleanup commit. |
| `013b901` | Slimmed branding assets to the mascot GIF. | Preserved the deletion of obsolete source images, generated previews, brand scripts, and planning docs. No Codex feature depended on those files. |
| `855b672` | Added the tagline line and hired-status coffee request in the Claude outcome command. | Preserved the tagline in the Codex README and converted the hired-status support note to the active `$application-outcome` skill with attribution to the original maintainer. |

## File Inventory

Final differences from official `8bee3dd` to `855b672`:

| Path | Status | Category | Codex/security notes |
|---|---:|---|---|
| `.claude/commands/outcome.md` | Modified | Legacy Claude workflow | Retained for parity comparison. Active Codex equivalent added to `$application-outcome`. |
| `.gitignore` | Modified | Ignore rules | Adds `.superpowers/` ignore coverage. No privacy regression. |
| `README.md` | Modified | Documentation/branding | Header image now points to `assets/mascot/pip_flight_loop.gif`; tagline added. Conflict resolved by keeping Codex instructions, privacy warnings, independent-project attribution, no-crypto disclaimer, and Codex skill workflow. |
| `assets/mascot/pip_flight_loop.gif` | Added | Binary image/GIF | Retained official mascot GIF only. Size is about 29 KB. No executable content or scripts. |
| `claude_animation.gif` | Removed | Obsolete binary image/GIF | Removed because the README no longer references it. |
| `tests/test_readme_assets.py` | Added | Test | Verifies local README image references resolve. |

Intermediate files introduced by `09f0417` and removed by `013b901`:

- `assets/mascot/README.md`
- `assets/mascot/assemble_flight_loop.py`
- `assets/mascot/make_brand_assets.py`
- `assets/mascot/pip_avatar.png`
- `assets/mascot/reference/A_standing_courier.png`
- `assets/mascot/reference/B_flying_delivery.png`
- `assets/mascot/reference/C_envelope_hugger.png`
- `assets/mascot/social_preview.png`
- `assets/mascot/sources/chatgpt_tie_sheet.png`
- `assets/mascot/sources/gemini_sheet.png`
- `docs/superpowers/plans/2026-07-12-pip-brand-pr.md`
- `docs/superpowers/specs/2026-07-11-mascot-brand-design.md`

These intermediate assets and scripts are not present in the final production
target and were not restored in the Codex branch.

## README Conflict Resolution

`README.md` conflicted because the Codex migration had replaced the original
Claude-oriented README while upstream added the mascot header to the official
README. The resolution keeps the Codex README and adds the official Pip GIF and
tagline at the top.

Preserved Codex README content:

- Codex installation and skill usage instructions.
- No Anthropic API key requirement.
- No Claude Code runtime requirement.
- Privacy warnings for local personal data.
- Independent MIT-licensed project attribution to Mads Lorentzen.
- Explicit statement that OpenAI, Anthropic, and job portals do not maintain or
  endorse this fork.
- No cryptocurrency, token, coin, or paid sponsorship disclaimer.
- Upstream-sync and verification documentation links.

## Codex Conversion

The official coffee request was added to the legacy Claude outcome command. The
active Codex conversion is in `.agents/skills/application-outcome/SKILL.md`:

- Trigger only when the recorded status is `hired`.
- Congratulate the user warmly first.
- Add the support note only once for the same application.
- Never add it for non-hired statuses.
- Attribute the coffee link to the original maintainer and the independent
  MIT-licensed project.

No application functionality changed for branding alone.

## Production Fixes Included

The combined sync branch starts from the verified production-fix branch and
therefore includes:

| Commit | Purpose |
|---|---|
| `d2e39f5` | Adds network-free Jobnet Bun helper test coverage. |
| `217bcd5` | Prevents `moderncv` achievement bullet clipping and adds a LaTeX contract test. |
| `b11a172` | Records production-verification documentation. |

## Security And Privacy Audit

Branding files were checked for:

- Executable content.
- Scripts retained in the final target.
- Credentials, tokens, cookies, or API keys.
- Tracking code.
- Unexpected large files.
- Cryptocurrency or token promotion.
- Machine-specific personal paths.

Result: no security or privacy issue identified in the final retained files.

## Verification Results

The following checks passed on `sync/upstream-2026-07-12-855b672` after the
official merge and Codex conversion:

| Check | Result |
|---|---|
| `python -m unittest discover -s tests -t . -v` | Passed: 90 tests in 64.987s |
| `python tools/lint_skills.py` | Passed: `lint_skills: OK (22 skills, 9 commands, settings.json)` |
| `python tools/security_guards.py` | Passed: `security_guards: OK (permissions allowlist, gitignore rules, package manifests)` |
| `python tools/codex_compatibility.py` | Passed: `codex_compatibility: OK (13 workflow skills, 8 shared references, AGENTS.md)` |
| `python tools/check_upstream_updates.py --no-fetch` | Passed: recorded official commit and `upstream/master` both `855b6725f34b52aa869a6d7e5fe39103dc56485f` |
| `git diff --check` | Passed |
| `git status --short` | Clean |

Additional checks:

- Bun install, typecheck, and tests passed for all six portal CLIs.
- CV compiled with `lualatex` to exactly two pages.
- CV rendered visual inspection passed; achievement bullets were not clipped.
- Cover letter compiled with `xelatex` to exactly one page.
- Cover-letter rendered visual inspection passed; signature was visible.
- `pdftotext -layout` ATS extraction passed with extractable contact text,
  headings, sensible reading order, and no replacement or mojibake markers when
  read as UTF-8.
- Temporary PDF/render/text outputs were kept under `%TEMP%` and removed after
  inspection.
- Final privacy scan before push remains required on the stable branch after the
  fast-forward merge.

## Later Official Commits

The release target is frozen at `855b672`. Any official commits published after
that target during this production cycle are intentionally deferred to the next
sync cycle.
