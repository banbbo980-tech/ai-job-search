# Upstream Update Report - 2026-08-26 - d1504d2

## Summary

Prepared official upstream changes through
`d1504d238844636f847e56565037632e6d8d6dd6` on temporary branch
`sync/upstream-2026-08-26-d1504d2`.

This branch preserves the Codex-native workflow and does not update
`docs/upstream-state.json`; that state file should be updated only after the
user approves fast-forwarding `codex-migration`.

## Official Commits Integrated

| Commit | Official change | Codex result |
|---|---|---|
| `8d27861` | Added missing `tools/` entries to the README file structure. | Added the missing tool descriptions to the Codex README documentation list without restoring the upstream Claude-first README body. |
| `d82df2f` | Fixed `/reset profile` so it also covers personalized evaluation and search-query files; expanded reset tests. | Preserved upstream `.claude/commands/reset.md` and tests, and ported the same privacy behavior into `$reset-job-profile` for the Codex local overlay, evaluation preferences, and search preferences. |
| `23dc193` | Added Python 3.10-3.14 matrix to CI Python tool tests. | Added the matrix while preserving the Codex PyYAML install required by `tools/codex_compatibility.py`. |
| `d1504d2` | Recorded the Python matrix change in `CHANGELOG.md`. | Preserved changelog entry. |

## Files Changed

- `.agents/skills/reset-job-profile/SKILL.md`
- `.claude/commands/reset.md`
- `.github/workflows/ci.yml`
- `CHANGELOG.md`
- `README.md`
- `tests/test_codex_upstream_parity.py`
- `tests/test_reset_command.py`
- `docs/upstream-updates/2026-08-26_d1504d2.md`

## Conflicts

| File | Resolution |
|---|---|
| `.github/workflows/ci.yml` | Kept the upstream Python version matrix and kept the Codex PyYAML install step. |
| `README.md` | Kept the Codex README as the active guide and added only the official missing `tools/` documentation entries. |

## Claude To Codex Conversion

| Official Claude change | Active Codex equivalent |
|---|---|
| `.claude/commands/reset.md` now treats `04-job-evaluation.md` and `search-queries.md` as personalized reset targets. | `$reset-job-profile` now explicitly inspects and clears the ignored local overlay under `documents/cv/codex_profile/`, including `search-preferences.md`, and names Codex evaluation/search preference areas so they are not mislabeled as framework-only data. |
| Upstream tests guard reset coverage for Claude files. | `tests/test_codex_upstream_parity.py` now guards the Codex reset skill for `documents/cv/codex_profile/`, `04-job-evaluation.md`, `search-queries.md`, and `search-preferences.md`. |
| Upstream CI tests Python 3.10-3.14. | Codex CI keeps the same Python matrix and keeps the Codex compatibility dependency installation. |

No Claude or Anthropic runtime requirement was restored.

## Verification Results

| Check | Result |
|---|---|
| `python -m unittest discover -s tests -t . -v` | PASS - 360 tests in 89.358s |
| `python tools/lint_skills.py` | PASS - 25 skills, 12 commands, settings.json |
| `python tools/security_guards.py` | PASS - permissions allowlist, hooks allowlist, gitignore rules, package manifests |
| `python tools/codex_compatibility.py` | PASS - 16 workflow skills, 10 shared references, AGENTS.md |
| `python tools/check_framework_version.py` | PASS |
| `python tools/check_upstream_updates.py --no-fetch` | Expected pending-update report because `docs/upstream-state.json` is intentionally unchanged until stable merge approval |
| `git diff --check` | PASS |
| Bun portal checks | PASS - Freehire 39, Jobbank 28, Jobdanmark 37, Jobindex 47, Jobnet 31, LinkedIn 41 |
| CV compile | PASS - `lualatex --disable-installer`, twice, `cv/main_example.tex` |
| Cover-letter compile | PASS - `xelatex --disable-installer`, twice, `cover_letters/cover_example.tex` |
| PDF page count | PASS - CV 2 pages, cover letter 1 page |
| Visual PDF inspection | PASS - no clipping, broken bullets, or out-of-bounds text on rendered CV page 1, CV page 2, or cover-letter page |
| ATS extraction | PASS - `pdftotext -layout -enc UTF-8`; contact placeholders and headings extract cleanly with no observed `(cid:)` or replacement-character artifacts |
| Privacy probes | PASS - ignored paths verified for local profile overlay, application archives, pasted postings, company cache, tracker, reports, generated CVs, and generated cover letters |

## Security And Privacy

- Work used only fictional/example template data.
- No personal CV, profile, tracker, posting, report, or generated application
  file was tracked.
- `git status --short` was clean after verification.
- Temporary ATS text and rendered PNG files were deleted from `%TEMP%`.
- Nothing was pushed.
- Nothing was merged into `codex-migration`.
- Nothing was pushed to `upstream`.

## Temporary Branch

- Branch: `sync/upstream-2026-08-26-d1504d2`
- Official upstream target: `d1504d238844636f847e56565037632e6d8d6dd6`
- Import/conversion commit before this report: `ceaf4a87624b42e56b85f971561fb5648422ce89`

## Merge Readiness

Safe to merge into `codex-migration` by fast-forward only after user approval,
provided a fresh pre-merge fetch confirms:

- `origin/codex-migration` has not changed unexpectedly.
- `upstream/master` remains at `d1504d238844636f847e56565037632e6d8d6dd6`.
- The worktree is clean.
- `python tools/security_guards.py` still passes.
